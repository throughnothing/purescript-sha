{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build",
    "test": "pulp test"
  },
  "dependencies": {
    "jssha": "^2.2.0",
    "rimraf": "^2.5.4",
    "text-encoding": "^0.6.1",
    "pulp": "^9.0.1",
    "purescript-psa": "^0.3.9",
    "purescript": "^0.10.1"
  }
}
